<h1>File Share</h1>
<p id="notice"><%= notice %></p>
    <% if @attachment.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this upload from being saved:</h2>

        <ul>
            <% @attachment.errors.each do |error| %>
            <li><%= error.full_message %></li>
            <% end %>
        </ul>
        </div>
    <% end %>

<%= link_to 'Back', attachments_path %>